#!/usr/bin/env node

/**
 * Homer — Autonomous agent TUI wrapping Claude Code.
 *
 * Opens a terminal dashboard with embedded Claude Code PTY.
 * Claims GitHub issues, spawns agents, shows live output.
 *
 * Usage:
 *   homer [--repo OWNER/REPO] [--agents N] [--tool claude]
 */

import { start } from "../lib/app.js";

start(process.argv.slice(2));
